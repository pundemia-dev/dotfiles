{
    "name": "dynamic",
    "flavour": "default",
    "mode": "dark",
    "variant": "neutral",
    "colours": {
        <* for name, value in colors *>"{{ name }}": "{{ value.default.hex_stripped }}"<* if {{ loop.last }} *><* else *>,<* endif *><* endfor *>
    }
}
